# ============================================================
# 电机智能故障诊断系统 — Python 依赖
# ============================================================
# 安装方式：pip install -r requirements.txt
#
# 核心依赖说明：
#   numpy + scipy    — 数值计算与信号处理（FFT、统计）
#   pandas           — 数据加载与清洗
#   scikit-learn     — 机器学习（Random Forest / XGBoost）
#   paho-mqtt        — MQTT 客户端（producer + consumer）
#   Flask + flask-cors — REST API 服务
#   joblib           — 模型序列化
#   matplotlib       — 图表输出（模型评估）
#   seaborn          — 高级可视化（混淆矩阵热力图）
# ============================================================

numpy>=1.24,<3.0
scipy>=1.10,<2.0
pandas>=2.0,<4.0
scikit-learn>=1.3,<2.0
paho-mqtt>=2.0,<3.0
Flask>=3.0,<4.0
flask-cors>=4.0,<7.0
joblib>=1.3,<2.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
